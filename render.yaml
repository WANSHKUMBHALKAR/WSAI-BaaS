services:
  - type: web
    name: wsai-backend
    env: docker
    plan: free
    docker:
      dockerfilePath: backend/Dockerfile
    envVars:
      - key: DATABASE_URL
      - key: SECRET_KEY
      - key: OPENAI_API_KEY
services:
  - type: web
    name: wsaibaas-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    plan: starter
    autoDeploy: true
    buildCommand: ""
    startCommand: ""
